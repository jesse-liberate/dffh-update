import React from 'react';
import ReactDOM from 'react-dom';

const Formbuilder = ()=>{

    return (
        <>
        this is the formbuilder
        </>
    );
}

const jsx = (
    <div id="react-formbuilder">
      <Formbuilder />
    </div>
)

ReactDOM.render(jsx, document.getElementById('mount-react-formbbuilder-page'));